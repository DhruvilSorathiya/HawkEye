<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>HawkEye Admin Setup</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="${docroot}/static/img/hawkeye-icon.png">
    <link href="${docroot}/static/css/HawkEye.css" rel="stylesheet">
    <link href="${docroot}/static/node_modules/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: linear-gradient(135deg, #17a2b8 0%, #138496 100%); min-height: 100vh; display: flex; align-items: center; justify-content: center; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        .setup-container { background: rgba(255,255,255,.95); border-radius: 20px; box-shadow: 0 20px 40px rgba(0,0,0,.1); padding: 40px; max-width: 520px; width: 100%; margin: 20px; }
        .setup-title { text-align: center; color: #333; margin-bottom: 10px; font-weight: 300; font-size: 2.0em; }
        .form-group { margin-bottom: 20px; }
        .btn-setup { background: linear-gradient(135deg, #17a2b8 0%, #138496 100%); border: none; color: #fff; padding: 12px 20px; border-radius: 6px; width: 100%; font-weight: 600; }
        .info { font-size: .95em; color: #555; margin-bottom: 15px; }
    </style>
    <script>
        function toggleKey() {
            var el = document.getElementById('setup_key');
            el.type = el.type === 'password' ? 'text' : 'password';
        }
    </script>
    <script type="application/ld+json">
        {"@context":"https://schema.org","@type":"WebPage","name":"HawkEye Admin Setup"}
    </script>
</head>
<body>
    <div class="setup-container">
        <h1 class="setup-title">First-time Admin Setup</h1>
        <p class="info">This page is only accessible when no admin accounts exist. Access is restricted to localhost or requires a valid setup key.</p>

        % if error:
        <div class="alert alert-danger"><i class="glyphicon glyphicon-exclamation-sign"></i> ${error}</div>
        % endif

        % if success:
        <div class="alert alert-success"><i class="glyphicon glyphicon-ok"></i> ${success}</div>
        % endif

        <form method="post" action="${docroot}/setup_admin">
            <div class="form-group">
                <label>Admin Username</label>
                <input type="text" name="username" class="form-control" placeholder="e.g. admin" required>
            </div>
            <div class="form-group">
                <label>Admin Password</label>
                <input type="password" name="password" class="form-control" placeholder="Choose a strong password" required>
            </div>
            <div class="form-group">
                <label>Email</label>
                <input type="email" name="email" class="form-control" placeholder="admin@example.com" required>
            </div>
            <div class="form-group">
                <label>Setup Key (if configured)</label>
                <div class="input-group">
                    <input type="password" id="setup_key" name="setup_key" class="form-control" placeholder="Enter setup key if required">
                    <span class="input-group-btn"><button type="button" class="btn btn-default" onclick="toggleKey()">Show</button></span>
                </div>
            </div>
            <button type="submit" class="btn btn-setup">Create Admin</button>
        </form>

        <div class="login-links" style="margin-top:15px;">
            <a href="${docroot}/login">Back to Login</a>
        </div>
    </div>

    <script src="${docroot}/static/node_modules/jquery/dist/jquery.min.js"></script>
    <script src="${docroot}/static/node_modules/bootstrap/dist/js/bootstrap.min.js"></script>
</body>
</html>



